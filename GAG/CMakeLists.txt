CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT (multi_hs_seq)

ADD_SUBDIRECTORY(test bin)

INSTALL(DIRECTORY config DESTINATION ${PROJECT_BINARY_DIR}/bin)
INSTALL(DIRECTORY data DESTINATION ${PROJECT_BINARY_DIR}/bin)

#ENABLE_TESTING()

